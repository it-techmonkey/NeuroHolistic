# Plan: Remove practitioner text, fix Zekra's image, fix "Where You Transform" font

## Summary
Three changes needed on the landing page:
1. Add Team section back with practitioners but remove the "Practitioner" text label
2. Fix Zekra Khayata's image (too zoomed in, cutting her head)
3. Fix "Where You Transform" font size to match other section headings

## Implementation

### 1. page.tsx - Add Team component back
Add import and component between ScientificFoundation and AcademyCTA:
```
import Team from "@/components/Team";
...
<Team />
```

### 2. Team.tsx - Restore practitioners without "Practitioner" text
- Add `practitioners` filter back
- Add practitioners grid section (from git history)
- Remove the `<span className="text-[10px]...">Practitioner</span>` span
- Fix Zekra's image: change `object-[center_22%]` to `object-[center_12%]` to show more headroom

### 3. TransformationAreas.tsx - Fix font size
Change line 54 from:
```tsx
className="text-3xl font-semibold tracking-tight text-slate-900 md:text-4xl"
```
To:
```tsx
className="text-[36px] font-semibold tracking-tight text-slate-900 md:text-[48px]"
```

## Verification
Run `npx tsc --noEmit` to verify no TypeScript errors
